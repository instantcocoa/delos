# Delos Configuration
# ====================
#
# This file documents all configuration options for Delos services.
#
# File Hierarchy:
#   .env           - Base config for Docker Compose (checked into repo)
#   .env.local     - Your local overrides and secrets (gitignored)
#
# To configure:
#   1. Copy your API keys to .env.local (create if needed)
#   2. The .env file has sensible defaults for Docker
#
# For local development (outside Docker), set service addresses:
#   DELOS_OBSERVE_ENDPOINT=localhost:9000
#   DELOS_RUNTIME_ENDPOINT=localhost:9001
#   etc.

# =============================================================================
# General Settings
# =============================================================================

# Environment name (development, staging, production)
DELOS_ENV=development

# Log level (debug, info, warn, error)
DELOS_LOG_LEVEL=debug

# Log format (text, json)
DELOS_LOG_FORMAT=text

# =============================================================================
# Database (PostgreSQL)
# =============================================================================

# For Docker: use service name "postgres"
# For local: use "localhost"
DELOS_DB_HOST=postgres
DELOS_DB_PORT=5432
DELOS_DB_USER=delos
DELOS_DB_PASSWORD=delos
DELOS_DB_NAME=delos
DELOS_DB_SSLMODE=disable

# Connection string format (alternative to individual settings)
# DELOS_DB_URL=postgres://delos:delos@localhost:5432/delos?sslmode=disable

# =============================================================================
# Redis
# =============================================================================

# For Docker: use service name "redis"
# For local: use "localhost"
DELOS_REDIS_URL=redis://redis:6379

# =============================================================================
# Service Endpoints
# =============================================================================

# These are how services find each other.
# For Docker: use service names (observe, runtime, prompt, etc.)
# For local: use localhost:PORT

DELOS_OBSERVE_ENDPOINT=observe:9000
DELOS_RUNTIME_ENDPOINT=runtime:9001
DELOS_PROMPT_ENDPOINT=prompt:9002
DELOS_DATASETS_ENDPOINT=datasets:9003
DELOS_EVAL_ENDPOINT=eval:9004
DELOS_DEPLOY_ENDPOINT=deploy:9005

# =============================================================================
# Observability / Tracing
# =============================================================================

# Enable OpenTelemetry tracing
DELOS_TRACING_ENABLED=true

# Sampling rate (0.0 to 1.0, where 1.0 = 100% of traces)
DELOS_TRACING_SAMPLING=1.0

# OTLP endpoint for traces (usually the observe service)
# DELOS_OTLP_ENDPOINT=observe:4317

# =============================================================================
# LLM Provider API Keys
# =============================================================================
# Add these to .env.local (gitignored) - DO NOT commit real keys!

# OpenAI
DELOS_OPENAI_API_KEY=

# Anthropic
DELOS_ANTHROPIC_API_KEY=

# Google (Gemini / Vertex AI)
# DELOS_GOOGLE_API_KEY=
# DELOS_VERTEX_PROJECT=
# DELOS_VERTEX_LOCATION=

# Other providers
# DELOS_OPENROUTER_API_KEY=
# DELOS_TOGETHER_API_KEY=
# DELOS_GROQ_API_KEY=

# Local models (Ollama)
# DELOS_RUNTIME_OLLAMA_ENABLED=true
# DELOS_RUNTIME_OLLAMA_URL=http://localhost:11434

# =============================================================================
# Service Ports (defaults, rarely need to change)
# =============================================================================

# DELOS_OBSERVE_PORT=9000
# DELOS_RUNTIME_PORT=9001
# DELOS_PROMPT_PORT=9002
# DELOS_DATASETS_PORT=9003
# DELOS_EVAL_PORT=9004
# DELOS_DEPLOY_PORT=9005
